<?xml version="1.0" encoding="utf-8"?>
<manifest package="org.viktorot.notefy"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".NotefyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".view.EditActivity"
                  android:label="Edit"
                  android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".notes_list.NotesListActivity"
            android:label="Notes Activity"/>

        <activity android:name=".view.DeletePopupActivity"
                  android:theme="@style/AppTheme.DeletePopup"
                  android:excludeFromRecents="true"
                  android:noHistory="true"/>
    </application>

</manifest>
